# ============================================================
# BACKUP SCRIPT CONFIGURATION (.env)
# ============================================================

# ------------------------------------------------------------
# 1. CẤU HÌNH CHUNG
# ------------------------------------------------------------

# Đường dẫn đến file chứa danh sách backup (Targets & Excludes)
# Mặc định: ./backup.conf (cùng thư mục với script)
BACKUP_CONF_FILE="./backup.conf"

# Tùy chọn nén (Compression)
# auto = Tự động chọn cái tốt nhất (zstd > pigz > gzip)
# zstd = Nhanh nhất (cần cài: apt install zstd)
# pigz = Nén đa luồng (cần cài: apt install pigz)
# gzip = Mặc định, chậm hơn nhưng có sẵn
COMPRESSION_ALGO="auto"
COMPRESSION_LEVEL="6"  # 1 (nhanh nhất) -> 9 (nén chặt nhất)

# Giữ lại file backup trên máy này sau khi gửi đi?
# true = Giữ lại, false = Xóa sau khi gửi thành công
KEEP_LOCAL=false

# Chế độ debug (in chi tiết log)
DEBUG=false

# ------------------------------------------------------------
# 2. REMOTE SERVERS (Nơi chứa backup)
# ------------------------------------------------------------
# Format: label:user@host:port:/remote/path|auth_method
# Auth methods:
# - SSH Key (Khuyên dùng): /path/to/private_key
# - Password: password:your_password

REMOTE_SERVERS="
# Server 1 (Dùng SSH Key - An toàn nhất)
vps_hanoi:root@192.168.1.50:22:/backups/daily|/home/user/.ssh/id_rsa

# Server 2 (Dùng Password - Nếu không có key)
# vps_hcm:backup@backup.example.com:2222:/storage|password:MatKhauBiMat123
"

# ------------------------------------------------------------
# 3. DATABASE BACKUP (Tùy chọn)
# ------------------------------------------------------------
# Hỗ trợ MySQL/MariaDB và PostgreSQL

DB_BACKUP_ENABLED=false
DB_TYPE=mysql        # mysql hoặc postgres
DB_HOST=localhost
DB_PORT=3306         # 3306 (mysql) hoặc 5432 (postgres)
DB_USER=root
DB_PASSWORD=your_db_password
DB_NAMES="db_name1 db_name2"

# ------------------------------------------------------------
# 4. THÔNG BÁO TELEGRAM (Tùy chọn)
# ------------------------------------------------------------
# Để nhận thông báo về tình trạng backup

TELEGRAM_BOT_TOKEN=""  # Ví dụ: 123456:ABC-DEF...
TELEGRAM_CHAT_ID=""    # Ví dụ: -1001234567890
